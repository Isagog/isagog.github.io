name: Build Static Site
on:
  push:
    branches: [master, develop]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Build static export
        run: pnpm build

      - name: Generate timestamp
        id: ts
        run: echo "value=$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      # Archive — keep a downloadable copy of every build
      - name: Upload static site
        uses: actions/upload-artifact@v4
        with:
          name: isagog-site-${{ steps.ts.outputs.value }}
          path: out
          retention-days: 7

      # Pages artifact — consumed by the deploy job below
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

  # GitHub Pages deploy — master only
  deploy:
    needs: build
    # if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # ---------------------------------------------------------------------------
  # Future: SSH/rsync deploy to your own host instead of (or alongside) Pages.
  # Store the private key in repo secrets as SSH_PRIVATE_KEY, plus host/user/path.
  #
  # deploy-ssh:
  #   needs: build
  #   if: github.ref == 'refs/heads/master'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Download static site
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: isagog-site-${{ needs.build.outputs.archive_name }}
  #         path: out
  #
  #     - name: Setup SSH key
  #       run: |
  #         mkdir -p ~/.ssh
  #         echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
  #         chmod 600 ~/.ssh/deploy_key
  #         ssh-keyscan -H "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts
  #
  #     - name: Rsync to server
  #       run: |
  #         rsync -avz --delete -e "ssh -i ~/.ssh/deploy_key" \
  #           out/ "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.SSH_PATH }}"
  # ---------------------------------------------------------------------------